# Changesets

This folder holds the pending release notes. Each file describes one change: how it bumps the
version, and what to say about it in the changelog.

Add one with `npx changeset` when a change is something a user of the package would notice.
Docs, CI and internal refactors do not need one.

Full documentation: https://github.com/changesets/changesets
